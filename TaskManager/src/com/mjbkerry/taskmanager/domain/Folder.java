package com.mjbkerry.taskmanager.domain;

/**
 * Represent a "Folder" which is a top level object, or container that itself is merely a container
 * @author mjb
 *
 */
public class Folder extends SecuredNode {
	// color, style?
}
